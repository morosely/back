<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="CommonMapper">

    <!--<delete id="delete" parameterType="JSONObject">
        delete from ${tableName}
        <where>
            <foreach collection="list" item="item" separator=",">
                (goodsCode = #{item.goodsCode} )
            </foreach>
        </where>
        </foreach>
    </delete>-->

    <select id="select" parameterType="JSONObject" resultType="JSONObject">
        select * from ${tableName} where dealStatus=0 limit 0,${pageSize}
    </select>

    <!-- <update id="updateDealStatus" parameterType="JSONObject">
        update ${tableName} set dealStatus = ${dealStatus}
        where ${keyName} in
        <foreach collection="keyValues" item="item" separator="," open="(" close=")">
           	${item}
        </foreach>
    </update> -->
    
    <update id="updateDealStatus" parameterType="JSONObject">
        <foreach collection="keyValues" item="item" separator=";" >
        	update ${tableName} set dealStatus = ${dealStatus} where ${keyName} = ${item}
        </foreach>
    </update>

    <select id="dataCount" parameterType="JSONObject" resultType="long">
        select count(1) from ${tableName} where dealStatus = 0;
    </select>
</mapper>