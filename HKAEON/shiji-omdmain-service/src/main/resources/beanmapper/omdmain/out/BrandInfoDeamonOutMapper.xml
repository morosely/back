<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="beanmapper.out.BrandInfoDeamonOutMapper">
  <resultMap id="BaseResultMap" type="java.util.Map">
    <!--
      WARNING - @mbg.generated
    -->
    <result column="entId" jdbcType="BIGINT" property="ent_id" />
    <result column="brandName" jdbcType="VARCHAR" property="name" />
    <result column="brandCode" jdbcType="VARCHAR" property="code" />
    <result column="erpCode" jdbcType="VARCHAR" property="jygs" />
  </resultMap>
  
  <select id="search" parameterType="map" resultMap="BaseResultMap">
		SELECT
		b.entId,b.erpCode,b.brandName,b.brandCode
		FROM
		brandinfo b where b.entId=#{entId}
		<if test="jygs != null">
			AND b.erpCode = #{jygs}
		</if>
		<if test="code != null">
			AND b.brandCode = #{code}
		</if>
		<if test="name != null">
			AND b.brandName = #{name}
		</if>
		<if test="page_no != null and page_size != null">
			limit ${page_no},${page_size}
		</if>
	</select>
	
	<select id="count" parameterType="map" resultType="long">
		SELECT
		count(1)
		FROM
		brandinfo b where b.entId=#{entId}
		<if test="jygs != null">
			AND b.erpCode = #{jygs}
		</if>
		<if test="code != null">
			AND b.brandCode = #{code}
		</if>
		<if test="name != null">
			AND b.brandName = #{name}
		</if>
	</select>
</mapper>